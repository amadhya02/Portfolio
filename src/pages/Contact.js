import React from 'react';

import { Container, Typography } from '@mui/material';

const Contact = () => (
  <Container>
    <Typography variant="h4">Contact Me</Typography>
    <Typography>Form or contact links will go here.</Typography>
  </Container>
);

export default Contact;
