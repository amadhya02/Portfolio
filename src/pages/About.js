import React from 'react';

import { Container, Typography } from '@mui/material';

const About = () => (
    <Container>
        <Typography variant="h4">About Me</Typography>
        <Typography>Short info, education, and hobbies go here.</Typography>
    </Container>
);

export default About;
